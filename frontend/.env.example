# Backend base URL (no trailing slash). Required for production builds when the UI is not served from the same origin as the API.
# Local dev: leave unset — Vite proxies /api to localhost:5000 (see vite.config.js).
VITE_API_URL=https://shi-jlk4.onrender.com
